<h1><%= @user.name %></h1>

<% @user.created_events.each do |event| %>
    <h3><%= event.description %> <%= "---Event Creator: #{@user.name}" %></h3
    <h5><%= event.date %></h5>
    <%end%>
<br>
<span><b><%= link_to 'Create Event', new_event_path %> </b></span>
