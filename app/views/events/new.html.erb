<h4>Create event</h4>
<% provide(:title, 'Create event') %>

<%= form_for @event do |f| %>
 
    <%= f.label :description%>
    <%= f.text_field :description %>

    <%= f.label :date %>
    <%= f.date_field :date%>
    <%= f.submit %>
    <% end%>